#include "monty.h"

/**
 * nop - nothing
 * @stack: stack head
 * @line_number: number of line
 * Return: no return
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) stack;
}
